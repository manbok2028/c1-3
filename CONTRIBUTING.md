# 기여 가이드

1. 변경 목적별 브랜치를 만든다.
2. 문서나 증빙을 추가하면 `evidence/manifest.toml`과 적합성 표를 함께 검토한다.
3. 민감정보가 보이는 캡처는 커밋하지 않는다.
4. `PYTHONPATH=src python -m codyssey_c1_3 validate`와 테스트를 실행한다.
5. PR에 변경 이유, 사용자 영향, 검증 결과, 남은 작업을 기록한다.

완료하지 않은 미션은 `pending`으로 유지하며 캡처나 실행 결과를 추정해 작성하지 않는다.
